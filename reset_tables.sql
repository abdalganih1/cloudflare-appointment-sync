DROP TABLE IF EXISTS appointments;
DROP TABLE IF EXISTS general_notes;
-- We keep 'users' table if it was created correctly, but let's check. 
-- Actually, the error was only about appointments. Users table was likely created fine as it was new.
